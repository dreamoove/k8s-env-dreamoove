apiVersion: v1
kind: Service
metadata:
  name: sport-clusterip-server
spec:
  type: ClusterIP
  selector:
    app: sport-service
  ports:
    - name: sport-service
      protocol: TCP
      port: 80
      targetPort: 80